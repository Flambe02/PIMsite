export default function LegacyDashboardContent({ locale }: { locale: 'br' | 'fr' }) {
  // Placeholder for the original dashboard layout
  return (
    <div>
      {/* TODO: Move the original legacy dashboard markup here */}
      <h1>Legacy Dashboard for {locale.toUpperCase()}</h1>
    </div>
  );
}
